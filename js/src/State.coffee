# State superclass for finitie state machine,
# although it's more like a prototype
class State
  constructor: (@name) ->
    
  doActions: ->
    
  checkConditions: ->
    
  entryActions: ->
  
  exitActions: ->